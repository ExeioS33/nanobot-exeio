[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = True
interpreter_python = auto_silent
# Uncomment once you've created .vault-pass (see README.md) — left commented
# so a fresh clone doesn't fail before the vault exists.
# vault_password_file = .vault-pass

[ssh_connection]
pipelining = True
